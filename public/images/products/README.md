# Product Images

Put product image files in this folder.

Use paths like this in `db.json` or the admin "Image path" field:

```text
/images/products/concrete-mixer.jpg
```

Supported browser formats include `.jpg`, `.png`, `.webp`, and `.svg`.

`product-placeholder.svg` is the default fallback image for tools without a custom product image.
