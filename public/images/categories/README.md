# Category Images

Put small category thumbnails in this folder.

Use paths like this in the admin category image field:

```text
/images/categories/building.jpg
```

`category-placeholder.svg` is the default fallback thumbnail.
